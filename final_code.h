#ifndef FINAL_CODE_H
#define FINAL_CODE_H

/**
 * @brief Verify user-entered code against expected code
 * @param expected_code The 2-digit code to verify against (0-99)
 * @return bool True if user entered correct code, False otherwise
 */
bool verify_final_code(int expected_code);

#endif